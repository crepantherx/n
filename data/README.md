# Runtime data folder

This folder is created and managed by the application. User accounts, per-user settings, uploaded resumes, logs, stats, debug screenshots, and agent memory live here during local use.

The production source zip intentionally does not include personal runtime data.
